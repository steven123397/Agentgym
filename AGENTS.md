# Agent 工作规则

## 工作顺序

1. 先阅读 `README.md`、`docs/TASK.md` 和现有目录。
2. 先创建 `docs/DESIGN.md`，不得在设计文档之前修改业务代码。
3. 根据 `docs/DESIGN.md` 实现任务。
4. 完成后运行测试和必要的启动检查。
5. 最终说明实际完成内容、验证结果和未确认事项。

## 修改边界

- 保持技术栈简单，不引入不必要的框架或依赖。
- 不要重写与任务无关的基座文件。
- 不要为了视觉效果添加后端、账号系统或远程服务。
- 需求不明确时，先提出最关键的问题，不要自行扩大范围。
- 设计文档必须反映真实实现，不能写一套方案、实现另一套方案。
- 项目文档统一放在 `docs/`，不要在根目录新增任务或设计文档。

## 验证要求

- 代码修改后至少运行 `npm test`。
- 如果修改了页面，尽量实际启动 `npm run dev` 并检查入口页面。
- 不要声称执行过没有实际执行的命令或验证。